# Launch Map-First QA

Status: DONE_WITH_CONCERNS. Local synthetic fixtures only; no deployment or messages.

- Branch: `codex/launch-map-first`, baseline `c797666`.
- Target: loopback fixture server, built Vite/MapLibre frontend.
- Scope: Map, Queue, Upcoming, launch details and data failure states.
- Driver: GStack headless Chromium; Aside unavailable. Isolated GPU-enabled
  browser used because the default macOS fallback disables WebGL.
- Fixtures labeled SYNTHETIC QA; no fixture proves real launch visibility.

## ISSUE-001: Legend Overlaps Status

Medium visual issue, reproduced twice at 390x844. On Map, legend bounds
805.6-832px overlap status footer 809.4-844px. The absolutely positioned legend
does not reserve space in the map layout. Expected: both labels remain readable.
Private local before evidence: `opd-qa-legend-before.png`.

Fixed in `0236a7d` and responsive re-verification passed. This issue cannot be detected by
the DOM-only unit suite; real browser bounding boxes exposed it.

## External Limitation

## ISSUE-002: Narrow Map Controls Overlap

Medium functional/visual issue, reproduced at 320px and 390px. Absolute time
controls cover All/Mine; at 320px the overlay row also clips off the left edge.
Expected: launch toggle and target filters remain individually reachable.
Before evidence: `opd-qa-map-320.png` and `opd-launch-map-mobile-final.png`.
Phone/tablet layouts now use grid rows for controls; wide desktop positioning is unchanged.
Fixes: `db19cd2`, `e9a2d3c`. Responsive re-verification passed.

## Verification Results

- 1,100 Python tests pass; measured generator coverage 89.76% (85% gate).
- 1,435 frontend tests and 160 Worker tests pass; both typechecks and Vite build pass.
- Real browser checks: Map, Queue, Upcoming and launch-detail navigation;
  320x740, 390x844, 768x1024 and 1280x900 layouts; nonblank WebGL imagery and
  gold launch markers; no horizontal overflow; legend above the status footer.
- Queue: two supported synthetic launches plus three Earth entries; Upcoming:
  four near-term candidates, excludes day-three candidate; Map includes day three.
  Same event/artifact revisions across cards; no ground percentage/star/window claims.
- Unknown trajectory, partial feed, stale wrapper, corrupt bytes and cold 503
  each leave five Earth Queue entries and zero launch entries.
- Warm 503 retains last-good facts with explicit labeling. Source-expiry tick
  removes launch Queue entries without a reload. Recovery fetch succeeds.
- Local cache-only shadow publication succeeds; no remote upload or message.
- Public diff scan has no credential findings. Reviewed false positives are
  four-component version numbers and analytic floating-point test constants.

## Fallback Policy And Limits

The existing reviewed contract permits still-fresh, clean, last-good launch
evidence in Queue until its source or artifact expires. A stricter ready-only
change was attempted and reverted after it failed that explicit policy test.
It is not counted as a verified fix. Choose a stricter policy before activating
real capture instructions, rather than silently changing the test contract.

Network emulation preserved navigator.onLine=true in this browser, exercising
the last-good path rather than proving the OS-offline path. Offline selection
and connectivity-transition behavior have unit coverage. Map marker-to-detail
entry has component coverage; a real canvas-marker click was not completed
because the browser driver's raw input method is disallowed. No workaround or
allowlist change was made. Queue/Upcoming detail clicks were browser-verified.

QA found two confirmed layout bugs and fixed both. One policy experiment was
reverted; no failing test changes remain. External imagery errors are deferred.
Scope score: 88 -> 91/100, rounded: console 40 due to eight external tile errors;
visual 84 -> 100, functional 92 -> 100, other exercised categories 100. This is
not a production, authenticated-profile, physical-device or optical-recall score.

## Ship Gate

Owner approved the separate lint cleanup and map-first ship/deploy. Commit
`4b05b0c` resolves the 16 existing Ruff findings through import cleanup, line
wrapping and a helper-name correction. Full Ruff, 1,100 Python tests (89.76%
coverage), 1,435 frontend tests, 160 Worker tests, both typechecks and the
production build pass again after that commit. Remote CI and production
readback are still required; this report records pre-deployment evidence.

## External Imagery Limitation

NASA GIBS polar Black Marble tiles return 404 in the browser fixture. This is an
existing external imagery boundary, not a launch JSON failure. Earth imagery,
track, launch markers and corridors render; do not call the console clean.

## Release Gates

No production or real-positive acceptance is claimed. Unattended publication,
remote ownership recovery, provider budgeting, trajectory/optical references,
map-only soak and explicit alert activation remain tracked in TODOS.md.
